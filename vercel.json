{
  "buildCommand": "cd artifacts/gorogly-girls && PORT=3000 BASE_PATH=/ pnpm run build && cp -r dist/public ../../public",
  "outputDirectory": "public",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
